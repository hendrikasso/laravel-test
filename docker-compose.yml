version: '3.7'
services: 
    app:
      build:
        context: .
        dockerfile: ./docker/Dockerfile
      image: 'dashboardapp'
      env_file:
        - ./local.env
      ports:
        - 8080:80
      